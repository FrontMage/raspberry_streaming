# Named Pipes

make a named pip for `netcat` to stream network sockets to pipe, then one can use `opencv` to open that pipe.

use `./stream.sh` on raspberry side

use `./make_pipe.sh` on remote machine

use `./display.py` to display video
